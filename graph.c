#include"proxy.h"
/**
  *	Each vertex corresponds to a host machine running a proxy.
  *	Each edge corresponds to a connection between two proxies.
  */
//	typedef struct{
	/**
	  *	Linked list of linked lists of a vertex's neighbor vertices
	  *	order of the graph.
	  */
//	} graph;
